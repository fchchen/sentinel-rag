# 2026-03-03

- Added the source blueprint and addendum `.docx` files to the workspace.
- Created a consolidated Markdown spec, an implementation checklist, and an initial backend/frontend/infra scaffold.
- Noted source gaps that still need explicit values: circuit-breaker thresholds, malware scanner mapping, evaluation sampling defaults, route reference, and env contract.
- Replaced the remaining spec gaps with concrete repo defaults in the consolidated spec and `.env.example`.
- Implemented Phase 1 auth foundations: bearer-token decoding, role guards, tenant-scoped audit endpoint, admin-only document delete, and admin-only policy override.
- Added auth-first tests and converted test transport to `httpx` ASGI; backend test suite is green.
- Installed backend dependencies with `uv` and frontend dependencies with `npm`; frontend production build succeeds.
- Implemented a Phase 2 slice: overridable token verifier abstraction, gateway router service, Redis-backed circuit-breaker store with in-memory fallback, and SQLAlchemy provider config persistence.
- Added Phase 2 tests for routing, verifier overrides, and persistence wiring; backend suite is now 14 passing tests.
- Switched local infra to host ports `5433` (Postgres) and `6380` (Redis) to avoid conflicts with other running containers.
- Started the local Postgres and Redis containers via `docker.exe compose`, initialized the schema, seeded `provider_configs`, and verified Redis breaker state persistence.
- Implemented a Phase 3 slice: policy engine with prompt injection blocking, PII redaction rules, and gateway integration that preserves raw prompts for the LLM while exposing redacted persistence output.
- Added a real signed JWT verifier path (`EntraJwtVerifier`) behind the existing auth boundary, backed by `PyJWT`, with local mode still available for test tokens.
- Added startup persistence bootstrap and Alembic scaffolding so schema initialization and provider seeding are no longer manual-only.
- Backend test suite is now 21 passing tests after the policy, JWT, and bootstrap additions.
- Added persisted `audit_logs`, `policy_violations`, and `documents` schema models plus an `AuditService` that records gateway requests and policy outcomes.
- Wired gateway requests to persist redacted prompts/responses and policy decisions, including blocked prompts with no completion payload.
- Added a first RAG foundation module with document lifecycle states and retrieval filtering logic.
- Extended the backend suite to 26 passing tests and verified the live Postgres container now contains `audit_logs`, `documents`, `policy_violations`, and `provider_configs`.
- Replaced the placeholder document route with real persisted `GET/POST/DELETE /api/v1/documents` behavior backed by `DocumentService`.
- Added `retrieval_runs`, `retrieval_results`, and `model_invocations` schema/models plus service coverage for retrieval persistence and gateway cost tracking.
- Backend suite is now 30 passing tests, and the live Postgres container now includes `model_invocations`, `retrieval_runs`, and `retrieval_results` as well.
- Added `POST /api/v1/retrieval/search` plus gateway-level retrieval integration so responses now carry `retrieval_run_id` and `retrieval_context`.
- Added persisted document scan transitions via `POST /api/v1/documents/{document_id}/scan-result`.
- Replaced the flat token-cost stub with provider/model-specific pricing in the audit flow; backend suite is now 35 passing tests.
- Upgraded retrieval ranking from a fixed placeholder order to keyword-strength reranking and added `GET /api/v1/retrieval/runs` for recent run inspection.
- Added a first persisted evaluation harness slice (`eval_results`) linked to `audit_log_id` and `retrieval_run_id`, using heuristic relevance/faithfulness scoring.
- Updated the frontend dashboard to surface documents and retrieval runs with API fetch + fallback data, and the backend suite is now 38 passing tests.
- Added persisted document chunking plus content-based retrieval scoring, so retrieval is no longer filename-only.
- Added quota-backed evaluation decisions (`tenant_quotas`) with sample/budget skip logic and new read APIs for `GET /api/v1/evals` and `GET /api/v1/metrics/costs`.
- Updated the frontend dashboard to surface eval results and model spend, and the backend suite is now 43 passing tests. The live Postgres schema now also includes `document_chunks` and `tenant_quotas`.
- Replaced filename-only retrieval signals with persisted chunk embeddings plus hybrid vector/keyword scoring, while storing chunk token counts and keyword signatures.
- Moved gateway evaluation off the request path by adding queued `eval_jobs`, persisted `COMPLETED` / `SKIPPED` eval result states, and an admin `POST /api/v1/evals/process` worker trigger.
- Added a local-only `POST /api/v1/auth/demo` endpoint and updated the React dashboard to bootstrap a real bearer token before loading protected API data.
- Backend suite is now 46 passing tests, and the frontend production build remains green after the auth-aware dashboard update.
- Verified the live Postgres runtime path outside the sandbox: `bootstrap_persistence()` completed successfully against the running `infra-postgres-1` container, and the real schema now exposes `eval_jobs` plus the reconciled `document_chunks` and `eval_results` columns.
- Replaced the default eval dispatcher with a real Celery-backed worker path: gateway completions now enqueue `eval_jobs` and submit Celery tasks, while the admin `POST /api/v1/evals/process` endpoint now queues backfill work instead of processing inline.
- Kept tests isolated by adding an inline eval worker override in `backend/tests/conftest.py`, so the suite still runs against in-memory SQLite without depending on Redis.
- Backend suite is now 49 passing tests after the Celery worker integration.
- Started a real local Celery worker against Redis, verified it consumed a queued backfill task, then submitted a fresh eval job end-to-end and confirmed the worker processed it to a persisted `COMPLETED` `eval_results` row in Postgres before shutting the worker down cleanly.
- Added persisted eval job retry metadata (`attempt_count`, `max_attempts`, `last_error`, `next_attempt_at`) plus exponential backoff and a terminal `FAILED` state for worker or broker failures.
- Fixed a real Postgres portability bug in the schema reconciler (`DATETIME` -> `TIMESTAMP`) while adding the new retry column.
- Backend suite is now 51 passing tests, and the live Postgres schema now includes the new retry columns on `eval_jobs`.
- Added `GET /api/v1/evals/jobs` plus dashboard support so operators can see live `COMPLETED` / `RETRY` / `FAILED` queue state with attempt counts and last errors.
- Added `eval_dead_letters` persistence and task-level Celery failure hooks so unrecoverable worker exceptions are captured with task payload and retry metadata.
- Backend suite is now 53 passing tests, the frontend build is green, and the live Postgres schema now includes `eval_dead_letters`.
- Added `GET /api/v1/evals/dead-letters` and `POST /api/v1/evals/jobs/{job_id}/requeue` so admins can inspect unrecoverable task failures and replay failed jobs after review.
- Updated the dashboard to surface dead-letter rows alongside eval job state.
- Backend suite is now 54 passing tests and the frontend build remains green.
- Added the remaining budget guardrails: tenant quotas now persist `monthly_llm_budget_usd`, `monthly_llm_spend_usd`, and `last_eval_reset_at`; gateway requests return `429` when monthly spend is exhausted; daily eval spend now resets after midnight UTC.
- Backend suite is now 56 passing tests, and the live Postgres schema now includes the new tenant quota budget columns.
- Added `month_bucket` rollover semantics so monthly tenant spend resets on the first quota read in a new UTC month instead of accumulating forever.
- Backend suite is now 57 passing tests, and the live Postgres schema now includes `tenant_quotas.month_bucket`.
- Completed the native Postgres retrieval path for the existing hybrid chunk index: document writes now refresh `document_chunks.embedding_pg` / `fts_tsv`, `RetrievalService` uses native `pgvector` + `ts_rank_cd` SQL on PostgreSQL with the SQLite fallback preserved, and the backend suite is now 59 passing tests.
- Verified the live Postgres schema outside the sandbox after this slice; `document_chunks` now exposes `embedding_pg` and `fts_tsv` on the running database.
- Extended the admin dead-letter API to include the stored task payload and updated the React dashboard so operators can review payload JSON inline and requeue failed jobs directly from the UI.
- Backend tests remain green at 59 passing, and the frontend production build remains green after the dead-letter operator workflow update.
- Replaced the inline heuristic eval scorer with a prompt-based judge runner: the evaluation worker now loads `backend/eval/prompts/faithfulness_v1.txt`, stores that prompt version in `eval_results.judge_version`, and executes through a provider-ready local judge abstraction using the `EVAL_JUDGE_MODEL` setting.
- Backend suite is now 61 passing tests after adding judge-runner coverage and swapping the evaluation path to the prompt-based judge contract.
- Updated the default eval judge model to `gpt-4.1-mini` in `sentinel-rag` config and docs.
- Switched the seeded Azure/OpenAI gateway provider defaults to `gpt-4.1-mini`, added matching pricing entries, and updated `seed_provider_configs()` so existing legacy `gpt-4o-mini` seeded rows are upgraded in place without clobbering unrelated custom provider choices.
- Verified the live Postgres `provider_configs` rows outside the sandbox after reseeding: `azure_openai` and `openai` now both resolve to `gpt-4.1-mini`.
- Wired the default gateway runtime to a LiteLLM-backed provider client and made `_build_gateway_service()` load persisted `provider_configs` instead of only in-memory defaults; tests now force `GATEWAY_PROVIDER_MODE=stub` so the backend suite stays offline.
- Added an explicit invalid-provider path so unsupported aliases return `422` instead of bubbling a `KeyError`, and the backend suite is now 66 passing tests.
- Switched the default `GATEWAY_PROVIDER_MODE` back to `stub` for the app runtime and `.env.example`, so real provider mode is now opt-in until secrets are configured.
- Replaced the fake `/api/v1/audit/logs` route with a real database-backed audit query over persisted `audit_logs`, and updated the auth isolation test to seed real rows through gateway calls instead of relying on hardcoded fixtures.
- Fixed document scan transitions so a missing document now returns `404 Document not found` instead of bubbling a 500, and the backend suite is now 67 passing tests.
- Implemented offline LiteLLM error classification for the gateway breaker path: `RateLimitError` now counts toward breaker state with the longer cooldown, non-`429` request/auth/content `4xx` errors do not increment the breaker, and this is covered without needing any Azure account or live provider calls.
- Backend suite is now 71 passing tests after adding provider-error classification coverage.
- Hardened the eval worker mutation path by adding an opaque `worker_token` on `eval_jobs`; Celery task dispatch, inline processing, broker-failure handling, and task-failure handling now require `(job_id, worker_token)` instead of mutating jobs by bare integer ID alone.
- Backend suite is now 72 passing tests after the worker-token hardening and reconciliation coverage update.
- Added audit response retention TTL support: `audit_logs` now persist `response_expires_at`, `AuditService` can filter audit queries by date range and purge expired `response_redacted` bodies, and `sentinel_rag.purge_expired_audit_responses` is now available as a Celery task for beat scheduling.
- Backend suite is now 74 passing tests after adding retention cleanup and audit date-range coverage.
- Added transparent envelope encryption for persisted audit text fields: `audit_logs.redacted_prompt` and `audit_logs.response_redacted` are now encrypted at rest via an app-configured `AUDIT_ENCRYPTION_KEY`, while the ORM continues to return decrypted values and legacy plaintext rows remain readable.
- Backend suite remains 74 passing tests after adding encryption-at-rest coverage for audit fields.
