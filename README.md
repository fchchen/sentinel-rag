# sentinel-rag

Initial workspace scaffold for the `sentinel-rag` enterprise RAG governance gateway.

This repository now includes:

- A consolidated Markdown specification merged from the Blueprint v2 and the addendum
- An implementation checklist and test-first build plan
- A starter project layout for the FastAPI backend, React frontend, and local infra

Key documents:

- `docs/sentinel-rag-spec-v2-consolidated.md`
- `docs/implementation-checklist.md`

The `.docx` source files remain in the repository as the original source material.
